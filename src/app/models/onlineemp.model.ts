export class onlineemp {
    constructor(private empname: string, private empid: string, private activeStatus: string, private computer: string, private timeworked: string, private ram: string,private app_name:String) { }
}